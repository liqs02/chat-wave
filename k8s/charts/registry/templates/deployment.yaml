apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.global.registry.name }}-deployment
  labels:
    app: {{ .Values.global.registry.name }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.global.registry.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.global.registry.name }}
    spec:
      containers:
        - name: {{ .Values.global.registry.name }}-container
          image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.port }}
