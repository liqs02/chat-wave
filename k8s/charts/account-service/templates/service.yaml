apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.account.service.name }}-service
spec:
  selector:
    app: {{ .Values.global.account.service.name }}
  ports:
    - name: {{ .Values.global.account.service.name }}-port
      protocol: TCP
      port: {{ .Values.service_port | default .Values.port }}
      targetPort: {{ .Values.port }}
