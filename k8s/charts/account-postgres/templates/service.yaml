apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.account.postgres.name }}-service
spec:
  selector:
    app: {{ .Values.global.account.postgres.name }}
  ports:
    - name: {{ .Values.global.account.postgres.name }}-port
      protocol: TCP
      port: {{ .Values.service_port | default .Values.port }}
      targetPort: {{ .Values.port }}
