apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.chat.service.name }}-service
spec:
  selector:
    app: {{ .Values.global.chat.service.name }}
  ports:
    - name: {{ .Values.global.chat.service.name }}-port
      protocol: TCP
      port: {{ .Values.service_port | default .Values.port }}
      targetPort: {{ .Values.port }}
