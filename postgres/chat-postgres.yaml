apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-postgres-deployment
  labels:
    app: chat-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-postgres
  template:
    metadata:
      labels:
        app: chat-postgres
    spec:
      containers:
        - name: chat-postgres
          image: postgres:alpine3.18
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: chat-postgres-configmap
                  key: db_name

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: chat-postgres-secret
                  key: username
                  
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: chat-postgres-secret
                  key: password
---
apiVersion: v1
kind: Service
metadata:
  name: chat-postgres-service
spec:
  selector:
    app: chat-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
